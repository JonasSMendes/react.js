import { OnlineUser } from "./OnlineUser"

export const Header = () => {
    return(
        <header>
            <h1>
                titulo da pagina
            </h1>
            
            <OnlineUser/>
        </header>
    )
}