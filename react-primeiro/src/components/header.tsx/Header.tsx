import { useContext } from "react"
import { YourName } from "../../contexts/NameContext"

export const Header = () => {   
    return(
        <header>
            <h1>
                Posts
            </h1>
        </header>
    )
}